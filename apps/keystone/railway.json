{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm run build",
    "watchPatterns": ["apps/keystone/**/*", "package.json"]
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
